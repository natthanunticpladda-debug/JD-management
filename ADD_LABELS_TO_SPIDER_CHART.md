# เพิ่มตัวเลขค่าบน Spider Chart

## ✅ การเปลี่ยนแปลง

เพิ่มตัวเลขแสดงค่าคะแนนของแต่ละจุดบน Spider Chart เพื่อให้อ่านค่าได้ง่ายขึ้น

---

## 🎨 ผลลัพธ์

### ก่อนแก้ไข:
```
        Execution
           /\
          /  \
         /    \
        /      \
```
(ไม่มีตัวเลขแสดง)

### หลังแก้ไข:
```
        Execution
           5        ← แสดงตัวเลข
           /\
          /  \
         /    \
        /      \
```

---

## 🔧 การเปลี่ยนแปลงโค้ด

### 1. เพิ่ม Tooltip Import

```typescript
import { 
  Radar, 
  RadarChart, 
  PolarGrid, 
  PolarAngleAxis, 
  PolarRadiusAxis, 
  ResponsiveContainer,
  Tooltip  // ← เพิ่ม Tooltip
} from 'recharts';
```

### 2. สร้าง Custom Label Component

```typescript
// Custom label component to show values on data points
const CustomLabel = (props: any) => {
  const { x, y, value } = props;
  return (
    <text
      x={x}
      y={y}
      dy={-10}              // ขยับขึ้นด้านบน 10px
      fill="#f59e0b"        // สีส้ม (เหมือนกับเส้น)
      fontSize={14}         // ขนาดตัวอักษร
      fontWeight="bold"     // ตัวหนา
      textAnchor="middle"   // จัดกึ่งกลาง
    >
      {value}
    </text>
  );
};
```

### 3. เพิ่ม Label และ Tooltip ใน Radar

```typescript
<Radar
  name="Score"
  dataKey="score"
  stroke="#f59e0b"
  fill="#fbbf24"
  fillOpacity={0.6}
  strokeWidth={2}
  label={<CustomLabel />}  // ← เพิ่ม label
/>

<Tooltip 
  contentStyle={{ 
    backgroundColor: 'rgba(255, 255, 255, 0.95)', 
    border: '1px solid #e5e7eb',
    borderRadius: '8px',
    padding: '8px 12px'
  }}
/>
```

---

## 🎨 การออกแบบ Label

### ตำแหน่ง:
- **dy={-10}**: ขยับขึ้นด้านบน 10px จากจุดข้อมูล
- **textAnchor="middle"**: จัดกึ่งกลางแนวนอน

### สี:
- **fill="#f59e0b"**: สีส้ม (Orange-500)
- เหมือนกับสีของเส้น stroke

### ขนาด:
- **fontSize={14}**: ขนาด 14px
- **fontWeight="bold"**: ตัวหนา

### ตัวอย่าง:
```
     5  ← Label (สีส้ม, ตัวหนา)
     ●  ← Data point
```

---

## 🎯 Tooltip

### การทำงาน:
- **Hover** เหนือจุดข้อมูล → แสดง tooltip
- แสดงชื่อ competency และคะแนน

### Style:
```typescript
contentStyle={{ 
  backgroundColor: 'rgba(255, 255, 255, 0.95)',  // พื้นขาวโปร่งใส
  border: '1px solid #e5e7eb',                   // ขอบสีเทาอ่อน
  borderRadius: '8px',                           // มุมโค้ง
  padding: '8px 12px'                            // Padding
}}
```

### ตัวอย่าง Tooltip:
```
┌─────────────────┐
│ Score           │
│ Execution: 5    │
└─────────────────┘
```

---

## 📊 ตัวอย่างการแสดงผล

### Spider Chart พร้อม Labels:

```
              Execution
                 5
                /|\
               / | \
              /  |  \
             /   |   \
            /    |    \
           /     |     \
          /      |      \
         /       |       \
    0  /        |        \  5
      /         |         \
     /          |          \
    /           |           \
   /            |            \
  /             |             \
 /              |              \
Long-term       |        Communication
Thinking        |
                |
                4
```

**แต่ละจุดจะมีตัวเลขแสดงค่าคะแนน**

---

## 🎨 Visual Hierarchy

### ลำดับความสำคัญ:
1. **Data Points** (จุดข้อมูล) - เด่นที่สุด
2. **Labels** (ตัวเลข) - เด่นรองลงมา
3. **Lines** (เส้น) - เชื่อมจุด
4. **Grid** (กริด) - พื้นหลัง
5. **Axes** (แกน) - ชื่อ competencies

### สี:
- **Labels**: `#f59e0b` (Orange-500) - เด่น
- **Lines**: `#f59e0b` (Orange-500) - เด่น
- **Fill**: `#fbbf24` (Amber-400) - โปร่งใส 60%
- **Grid**: `#e5e7eb` (Gray-200) - อ่อน
- **Axes**: `#6b7280` (Gray-500) - ปานกลาง

---

## 📱 Responsive Behavior

### Desktop:
- Labels แสดงชัดเจน
- ไม่ทับกัน

### Mobile:
- Labels อาจจะเล็กลง
- ยังคงอ่านได้

---

## 🧪 การทดสอบ

### Test Case 1: Competency มีคะแนน 5
**Expected**: แสดงตัวเลข "5" เหนือจุดข้อมูล

### Test Case 2: Competency มีคะแนน 0
**Expected**: ไม่แสดงจุดข้อมูล (ถูกกรองออก)

### Test Case 3: Hover เหนือจุดข้อมูล
**Expected**: แสดง tooltip พร้อมชื่อและคะแนน

### Test Case 4: Print
**Expected**: Labels พิมพ์ออกมาด้วย

---

## 🎨 Customization Options

### เปลี่ยนตำแหน่ง Label:
```typescript
dy={-10}  // ขยับขึ้น/ลง (ค่าลบ = ขึ้น, ค่าบวก = ลง)
dx={0}    // ขยับซ้าย/ขวา (ค่าลบ = ซ้าย, ค่าบวก = ขวา)
```

### เปลี่ยนสี Label:
```typescript
fill="#your-color"  // เปลี่ยนสี
```

### เปลี่ยนขนาด Label:
```typescript
fontSize={16}       // เปลี่ยนขนาด
fontWeight="normal" // เปลี่ยนความหนา
```

### เปลี่ยน Tooltip Style:
```typescript
<Tooltip 
  contentStyle={{ 
    backgroundColor: '#your-bg-color',
    border: '1px solid #your-border-color',
    // ... other styles
  }}
/>
```

---

## 📊 Data Flow

```
Competency Data
    ↓
Filter (score > 0)
    ↓
Transform to Chart Data
    ↓
Render Radar Chart
    ↓
Render Data Points
    ↓
Render Labels (CustomLabel)
    ↓
Show Values on Chart
```

---

## 🎯 Benefits

### 1. อ่านค่าได้ง่ายขึ้น
- ไม่ต้องประมาณค่าจากกริด
- เห็นค่าที่แน่นอน

### 2. เปรียบเทียบได้ง่าย
- เห็นความแตกต่างชัดเจน
- ไม่ต้องนับกริด

### 3. Professional Look
- ดูเป็นมืออาชีพ
- ข้อมูลครบถ้วน

### 4. Print-Friendly
- Labels พิมพ์ออกมาด้วย
- อ่านง่ายบนกระดาษ

---

## 📝 หมายเหตุ

### Label Positioning:
- Labels อยู่เหนือจุดข้อมูล (dy={-10})
- ถ้าจุดข้อมูลใกล้กันมาก labels อาจทับกัน
- สามารถปรับ dy ได้ตามต้องการ

### Tooltip:
- แสดงเมื่อ hover เท่านั้น
- ไม่แสดงเมื่อ print
- รองรับ touch devices

---

## ✅ สรุป

**เพิ่มตัวเลขค่าบน Spider Chart เรียบร้อยแล้ว!**

- ✅ แสดงค่าคะแนนที่แต่ละจุด
- ✅ สีส้มเด่นชัดเจน
- ✅ ตัวหนาอ่านง่าย
- ✅ มี Tooltip เมื่อ hover
- ✅ Print-friendly

**ตอนนี้อ่านค่าได้ง่ายขึ้นมาก!** 📊
